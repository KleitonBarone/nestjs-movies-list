# Application
PORT=3000
NODE_ENV=production
JWT_SECRET=change-me

# Postgres (compose will provision `db` with these)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=movies
